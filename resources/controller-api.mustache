{{>partial_header}}
package {{packageName}}

{{#operations}}

import (
	"net/http"
    server "swagger-codegen-example/server"
)

{{#operation}}

func {{nickname}}(w http.ResponseWriter, r *http.Request) {
    h := server.InnerHandlerMap["{{nickname}}"]
    if (h != nil) {
        h(w,r)
        return
    }
	w.Header().Set("Content-Type", "application/json; charset=UTF-8")
	w.WriteHeader(http.StatusOK)
}

{{/operation}}

{{/operations}}
